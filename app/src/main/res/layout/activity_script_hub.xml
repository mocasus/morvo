<?xml version="1.0" encoding="utf-8"?>
<!-- Morvo — Script Hub (brutalist dark) -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#0a0a0a"
    android:padding="16dp">

    <!-- Header -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:fontFamily="monospace"
        android:text="╔══════════════════╗\n║  SCRIPT HUB      ║\n╚══════════════════╝"
        android:textColor="#2ea44f"
        android:textSize="14sp"
        android:gravity="center"
        android:lineSpacingExtra="0dp"
        android:layout_marginBottom="12dp"/>

    <!-- Category -->
    <TextView
        android:id="@+id/tvHubCategory"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:fontFamily="monospace"
        android:text="// ALL SCRIPTS"
        android:textColor="#6e7681"
        android:textSize="11sp"
        android:layout_marginBottom="8dp"/>

    <!-- Script list -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvScripts"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:scrollbars="vertical"/>

    <!-- Footer hint -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:fontFamily="monospace"
        android:text="> Tap any script to copy to clipboard"
        android:textColor="#30363d"
        android:textSize="10sp"
        android:gravity="center"
        android:layout_marginTop="8dp"/>

</LinearLayout>
